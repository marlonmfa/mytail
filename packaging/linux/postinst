#!/bin/sh
set -e
mkdir -p /etc/mytail
chmod 700 /etc/mytail
systemctl daemon-reload
systemctl enable --now mytail-agent.service
printf '%s\n' 'MyTail instalado. Abra http://127.0.0.1:8787 para configurar.'
